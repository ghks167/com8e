<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
  <!--	prod.xml -->
  
<mapper namespace="com.com8e.prod.dao.IProdDao">
	
	<insert id="insertProd" parameterType="com.com8e.prod.vo.ProdVO">
		<selectKey keyProperty="prod_no" resultType="int" order="BEFORE">
				select prod_seq01.nextval from dual
		</selectKey>
		INSERT INTO prod (
	    prod_no,
	    prod_name,
	    prod_price,
	    prod_com,
	    prod_qty,
	    prod_type,
	    prod_date
		) VALUES (
	    #{prod_no},
	    #{prod_name},
	    #{prod_price},
	    #{prod_com},
	    #{prod_qty},
	    #{prod_type},
	    sysdate
		)
	</insert>
	
	 
	
	<select id="selectProdType"
			 resultType="com.com8e.prod.vo.ProdVO">
		select distinct prod_type from prod
	</select>	
	
	
	<select id="selectProdList" parameterType="com.com8e.prod.vo.ProdVO">
		SELECT		prod_no,
				    prod_name,
				    prod_price,
				    prod_com,
				    prod_qty,
				    prod_type,
				    TO_CHAR(prod_date, 'YYYY.MM.DD') AS prod_
		FROM PROD
	</select>
	
</mapper>